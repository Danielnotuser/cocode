version: '3.8'

services:
  app_instance1:
    build: .
    container_name: myapp-1
    ports:
      - "8081:8080"

  app_instance2:
    build: .
    container_name: myapp-2
    ports:
      - "8082:8080"

